- RootLayout
  Add generic title instead of the hardcoded "Buscador de Abrigados em Canoas"
- Hamburger + Modal as menu responsive
- Loader component with theme colors
- hightlight "oao it och"


const fullName = removeDiacritics(fullNameProp).toLowerCase().trim();
  const highlightPatterns = removeDiacritics(highlightNames)
    .toLowerCase()
    .trim()
    .split(" ");

font-bold dark:bg-destructive bg-yellow-300